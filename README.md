# Nagios Plugin: check\_rest\_api

## Dependencies:
- `libcurl`
- `JSON-c`

## Compiling:
- Make sure your dependencies are installed
- `make`

## Use:
- Drop the executable into the `libexec` folder of your Nagios installation (like `/usr/local/nagios/libexec`)
- Options: TODO
